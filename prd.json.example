{
  "project": "MyApp",
  "branchName": "ralph/task-selection",
  "description": "Task selection by dependency/flow without stored priority",
  "constraints": [
    "All dependency installation, linting, testing, builds, and database seeding must run inside containers (Docker/Podman/Compose); do not install toolchains on the host"
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "Select next task by dependency/flow",
      "description": "As an agent, I need to choose the next task dynamically based on dependencies and implementation flow without stored priority fields.",
      "acceptanceCriteria": [
        "Task selection process uses dependency/implementation flow at iteration start",
        "No priority field is stored in PRD or tasks",
        "Typecheck passes via containerized tooling only"
      ],
      "passes": false,
      "notes": ""
    }
  ]
}
