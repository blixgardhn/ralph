PRD + Ralph tasks.json skill lives here.
`SKILL.md` describes generating both a markdown PRD (saved to `.ralph/prds/…`) and a corresponding `tasks.json` (saved to `.ralph/tasks.json`) where tasks realize and support the PRD's requirements.
