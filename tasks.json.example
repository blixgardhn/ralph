{
  "project": "MyApp",
  "branchName": "ralph/task-selection",
  "description": "Task selection by dependency/flow without stored priority",
  "constraints": [
    "All dependency installation, linting, testing, builds, and database seeding must run inside containers (Docker/Podman/Compose); do not install toolchains on the host"
  ],
  "tasks": [
    {
      "id": "T-001",
      "title": "Select next task by dependency/flow",
      "description": "Choose the next task dynamically based on dependencies and implementation flow without stored priority fields.",
      "acceptanceCriteria": [
        "Task selection uses dependency/implementation flow at iteration start",
        "No priority field is stored in PRD or tasks",
        "Typecheck passes"
      ],
      "keyFiles": [
        "src/task-selector.ts",
        "src/models/task.ts",
        "tests/task-selector.test.ts (create new)"
      ],
      "implementationNotes": "Use topological sort on dependsOn fields to determine order. Follow existing patterns in the codebase for reading tasks.json.",
      "passes": false,
      "notes": ""
    }
  ]
}
